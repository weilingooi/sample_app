<%= link_to gravatar_for(current_user), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t(".profile"), current_user %></span>
<span><%= pluralize current_user.micro_posts.size, t("microposts.title") %></span>
